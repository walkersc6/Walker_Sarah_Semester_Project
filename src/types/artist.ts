// artist type

// import type { Track } from './track.ts'

export type Artist = {
    id: number,
    name: string,
    picture: string,
    number_of_albums: number,
    tracklist: string,
    // artist_top_tracks: Track[]
}